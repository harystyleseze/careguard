# Runtime Data

This directory is for local runtime state generated by CareGuard services.

Do not commit generated JSON files from this directory. Files such as
`spending.json` and `orders.json` can contain balances, transaction history,
recipient details, and other sensitive payment context.

If you need sample data for tests or documentation, add a sanitized fixture
under the relevant test fixture directory instead of committing runtime data.
